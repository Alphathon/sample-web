After creating the run.sh file, make it executable by running the following command in the terminal:

bash
chmod +x run.sh

Now you can run the script by executing the following command:

bash
./run.sh